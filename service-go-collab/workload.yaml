apiVersion: openchoreo.dev/v1alpha1

metadata:
  name: doclet-collab-svc

endpoints:
  - name: ws
    port: 8080
    type: HTTP
    schemaFile: asyncapi.yaml

dependencies:
  resources: 
    - ref: doclet-nats
      envBindings:                 
        url: DOCLET_NATS_URL
